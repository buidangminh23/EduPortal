# Ignored directories
Web/node_modules
Web/dist
Web/dist-ssr

# System files
.DS_Store
Thumbs.db

.vercel

# Agent / design handoff artifacts
.claude/
Web/.claude/
_design_extract/

# Added by `vercel link`. Web/.gitignore re-includes the two env files that are
# committed on purpose (.env.example, .env.development) — a nested negation
# wins over this, so both patterns can stay.
.env*
